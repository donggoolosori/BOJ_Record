for (let i = n - 1; i >= (n / 2) >> 0; i--) {
  //   let curr = [n, i, n - i];
  //   while (true) {
  //     const next = curr[curr.length - 2] - curr[curr.length - 1];
  //     if (next < 0) break;
  //     curr.push(next);
  //   }
  //   if (curr.length > res.length) {
  //     res = [...curr];
  //   }
  // }
